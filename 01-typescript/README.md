# 01 - TypeScript Fundamentals

Work through these in order. Ask Copilot for hints only unless you want full solutions.

## Competencies

- Primitive/object/array typing
- Union types and narrowing
- Type aliases and interfaces
- Generics
- Utility types (`Pick`, `Omit`, `Partial`, `Readonly`)
- Async function return types

## Tasks

1. Complete TODOs in `exercises.ts`.
2. Add your own `ApiResult<T>` generic.
3. Refactor one interface into a type alias and explain why.
4. Add a function that safely parses unknown JSON.

## Stretch

- Add strict error handling to async functions with typed result objects.

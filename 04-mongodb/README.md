# 04 - MongoDB Fundamentals

Use `mongosh` or MongoDB Compass playground mode.

## Competencies

- Document modeling
- Insert/find/update/delete
- Update operators (`$set`, `$inc`, `$push`)
- Query filters and projections
- Aggregation basics (`$match`, `$group`, `$sort`)

## File

- `seed-and-queries.mongodb.js`

## Tasks

1. Run the seed section.
2. Complete TODO queries and updates.
3. Add one aggregation that calculates done ratio per user.
